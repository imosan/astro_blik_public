
<button id="to-page-top" class="to-page-top" onclick="toTop()"/>

<script is:inline>
    toTop = () => {
			window.scrollTo({
                top: 0,
                behavior: "smooth"
            })}
</script>

<style>
.to-page-top {
    background: #d6d4d0;
    border: none;
    cursor: pointer;
    outline: none;
    padding: 0;
    appearance: none;
    opacity: 0.6;

    position: fixed;
    right: 30px;
    bottom: 30px;
    z-index: 1000;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
}

.to-page-top::before {
    content: "";
    width: 12px;
    height: 12px;
    margin-bottom: -6px;
    border-top: solid 3px #fff;
    border-right: solid 3px #fff;
    transform: rotate(-45deg);
}

.to-page-top:hover {
    transform: scale(1.1);
}