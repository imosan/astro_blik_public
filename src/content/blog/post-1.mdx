---
title: 'SecondEntry'
pubDate: 2023-12-08
description: 'SecondEntry'
author: "XXX"
image: ""
url: ""
alt: 'SecondEntry'
tags: ["astro", "tag2", "tag3"]
seqNum: 0
---
import BlogImage from '../../components/BlogImage.astro';
import BlogVideo from '../../components/BlogVideo.astro';

## セットアップ手順

すでに Ubuntu と、そこに node がインストールされている環境で以下を実行する。

astro_blic_publicレポジトリをクローンする。

```sh

git clone https://github.com/imosan/astro_blik_public.git

```
クローンしたディレクトリに入る。
```sh

cd astro_blik_public

```
すべてのパッケージをインストールする。
```sh

npm install

```
pnpmならば、
```sh

pnpm install

```
ローカルで起動する。
```sh

npm run dev

```
最後に、表示されたURLをブラウザで開く。通常は <a href="http://localhost:4321">localhost:4321</a>


## vercelにデプロイする

```sh

vercel

```

Vercel CLI 32.5.5
? Set up and deploy “/mnt/c/Development/Firebase/astro_blik_public”? [Y/n]
